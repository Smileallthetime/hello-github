#学习日志
---

## 2026-09-11 · 第一次用 Git 和 GitHub

**今天做了什么**

- 注册了 GitHub 账号

- 配置好了 Git 的身份，学会了第一次 `commit`

- 把练习仓库推到了 GitHub 上


**卡住的地方**

- 网络报错，github.com连不上，是DNS给的IP被阻断，让deepseek harness换个IP就通了

- 第一次上传git add后面没有加 . 

- git commit -m 说明  这条后面应该用""把说明括起来


**搞懂了什么**

-工作区 你改的文件→git add .→暂存区 待提交篮子→git commit -m "说明"→本地仓库 在自己电脑中→git push→GitHub 云端网站
                                                                                                                                                                           ←git pull  ←

-`cd 文件夹路径`   进入某个文件夹 —— 每个新窗口的第一步

-`git add .`   把改动放进待提交的篮子 —— 准备存档之前

-`git commit -m "说明"`   在你电脑上存一个版本快照 —— 写完一段能跑的代码

-`git push`   把快照上传到 GitHub —— 一天结束时

-`git status`   看完现在改了什么、什么状态 —— 迷茫可以敲一下

-add 后面必须有空格，再跟 .

-commit -m 后面用英文双引号把说明包起来。养成习惯，以后说明里有空格也不会出问题

-还有一些

git pull	把 GitHub 上的最新版拉回本地	换电脑、和别人协作前

git log --oneline	列出所有存档点	回顾历史、准备回退

git restore 文件名	把文件恢复到上次存档的状态	改坏了要救命时

git clone 网址	把别人的项目整个复制到本地	下载别人的代码来学习

git init	把普通文件夹变成 Git 仓库	自己从零开一个新项目

git config --global	配置你的名字和邮箱	一台新电脑只做一次


**下次想搞明白**

- 分支（branch）到底是干嘛的，什么时候需要它（deepseek生成的）

---

## 2026-09-15 · Oracle 多表关联（JOIN）

**今天做了什么**

- 把上次关掉的 Docker 和 Oracle 重新拉起来（数据一条没丢，因为在 `oracle-free-data` 卷里）
- 建了第二张表 `study.维修工单`：8 张工单，故意留了两个「缺口」
  - `SB-004` 一次都没修过（设备有、工单没有）
  - `GD-008` 的设备编号 `SB-099` 在设备台账里根本不存在（工单有、设备没有）
- 学会了两张表拼起来查，一共做了 9 段练习，每段都真跑过、对过结果
- 新文件：`oracle/04-join.sql`（讲解 + 9 段可跑的例子）、`oracle/05-join-practice.sql`（练习本）、`oracle/05-join-practice-答案.sql`（答案）、`oracle/环境速查.md`（每次开机怎么把环境拉回来）

**卡住的地方**

- 一张表变成两张表以后，写列名要带「小名」（别名），`d.设备编号`、`w.设备编号` —— 不写就不知道你说的是哪张表的
- 一开始想不明白：为什么 `JOIN` 出来的不是 5 行（设备数）也不是 8 行（工单数），而是 7 行
- 最容易踩的坑：`LEFT JOIN` 之后把右表的条件写进 `WHERE`，`LEFT JOIN` 就白写了

**搞懂了什么**

- 为什么要拆表：一台设备会修很多次，维修记录要是跟设备信息塞一张表，医院名、型号就得重复写很多遍，改一处得改十几行还容易漏
- `JOIN` 的骨架，一个字一个字都有用：
  ```sql
  SELECT d.设备编号, w.故障现象
  FROM   study.设备台账 d          -- d 是这张表的小名
  JOIN   study.维修工单 w          -- JOIN = 把两张表拼起来
    ON   d.设备编号 = w.设备编号    -- 拼接规则：两边设备编号相等（关联条件）
  ```
- `JOIN` 默认 = `INNER JOIN`（内连接）＝「只留两边都对得上的行」
  - 7 行 = 8 张工单里减掉 `GD-008`（设备不存在），再加上没工单的 `SB-004` 也不出现
  - 两个缺口各掉一个，正好 8 - 1 = 7
- `LEFT JOIN`（左连接）＝「左边的表一行都不能少」，对不上就填 `NULL`
  - `设备台账 LEFT JOIN 维修工单` → 8 行，`SB-004` 也出来了，工单那几列是空的
- **一句话记住的坑**：`LEFT JOIN` 之后，**右表的条件写在 `ON` 里，别写在 `WHERE` 里**
  - 写 `WHERE w.完成状态 = '已完成'` → 只剩 5 行，`SB-004`、`SB-005` 全没了，`LEFT JOIN` 退化成了 `JOIN`
  - 原因是：对不上的行 `w.完成状态` 是 `NULL`，`WHERE` 拿 `NULL` 跟 `'已完成'` 比，比不成立，整行被删掉
  - 改写到 `ON` 里（`ON d.设备编号 = w.设备编号 AND w.完成状态 = '已完成'`）→ 7 行，5 台设备全在
- `LEFT JOIN` 最实用的一招：**找出「对不上」的行** —— 接 `WHERE 右表主键 IS NULL`，就能捞出「从没修过的设备」和「设备台账里查不到的野工单」（各 1 条，正是自己埋的那两个缺口）
- 空值只能用 `IS NULL` 判断，不能写 `= NULL`
- `COUNT(*)` 和 `COUNT(某列)` 不一样：`LEFT JOIN` 之后 `COUNT(*)` 会把空行数成 1（`SB-004` 变 1 次，错），`COUNT(w.工单号)` 空值不数（`SB-004` 是 0 次，对）
- 用自己工作里的东西做例子，查询一下就「有用」了：
  - 故障排行榜：柜门锁打不开 3 次、触摸屏黑屏 2 次、读卡器不识别工牌 2 次、通信中断离线 1 次
  - 每台设备维修次数、每个处理人处理多少张（张工 3、李工 3、王工 2）
  - 「已停用但还有处理中工单」的设备 → `SB-002`（这种就是要盯的）
- 学到的通用习惯：「先建新的 → 验证 → 再删旧的」；脚本要写成能反复跑的（`DROP` 包在 `BEGIN ... EXCEPTION WHEN OTHERS THEN NULL; END;` 里）

**下次想搞明白**

- 三张表一起连（`JOIN` 后面继续接 `JOIN`），比如再加一张「医院表」或「配件表」
- 子查询（一条 SQL 套一条 SQL），以及 `JOIN` 和子查询什么时候用哪个
- 能不能把工单数据真存进 Oracle，以后日报 / 周报用 SQL 一键生成出来
