-- Oracle 练习本：多表关联（JOIN）
-- 用法：在每题的「你的答案」处写 SQL，存盘后单独执行（DBeaver 里选中按 Alt+X）
--       先做不出来也没关系，把想法写下来，再看 04-join.sql 里的写法对照。

ALTER SESSION SET CONTAINER = FREEPDB1;
SET LINESIZE 160
SET PAGESIZE 100

PROMPT
PROMPT ===== 动手前：两张表现状 =====
SELECT COUNT(*) AS 设备数 FROM study.设备台账;
SELECT COUNT(*) AS 工单数 FROM study.维修工单;



-- ==========================================================
-- 任务 1：每张工单 + 它的设备型号 + 医院名称，按工单号排序
--   两个思路：
--     A. 还从设备台账出发：FROM study.设备台账 d JOIN study.维修工单 w
--     B. 也可以从工单出发：FROM study.维修工单 w JOIN study.设备台账 d
--   注意：两台表都有「设备编号」，写列的时候要带别名（d. / w.）
--   参考骨架：
--     SELECT w.工单号, d.型号, d.医院名称
--     FROM   study.维修工单 w JOIN study.设备台账 d
--       ON   w.设备编号 = d.设备编号
--     ORDER  BY w.工单号
-- ==========================================================
--
-- ↓ 你的答案 ↓



-- ↑ 答案结束 ↑



-- ==========================================================
-- 任务 2：查出所有「处理中」的工单，带上设备编号和型号
--   提示：完成状态在工单表（w）里，型号在设备表（d）里
--        在 JOIN 后面加 WHERE w.完成状态 = '处理中'
--   想一想：这里用 JOIN 还是 LEFT JOIN 有区别吗？为什么？
-- ==========================================================
--
-- ↓ 你的答案 ↓



-- ↑ 答案结束 ↑



-- ==========================================================
-- 任务 3：SB-001 这台设备修过几次、都是什么毛病
--   提示：WHERE d.设备编号 = 'SB-001'
--        要看到「几次」得用 COUNT，再加 GROUP BY
--   想简单点先只列工单也行，一样算对
-- ==========================================================
--
-- ↓ 你的答案 ↓



-- ↑ 答案结束 ↑



-- ==========================================================
-- 任务 4：找出「状态是已停用、但还有处理中工单」的设备
--   这是真实工作里要盯的：已经停用的设备还有没修完的活
--   提示：这个必须从设备台账出发用 LEFT JOIN
--        （想清楚为什么不能从工单出发）
--        WHERE d.状态 = '已停用'  AND  w.完成状态 = '处理中'
--   ⚠️ 上一题的教训：右表的条件写在 ON 里还是 WHERE 里，结果不一样。
--      这题两个条件都是「筛掉不要的」，写 WHERE 里就对了 ——
--      想想为什么这题跟 04 里 ⑦ 的坑不是一回事。
-- ==========================================================
--
-- ↓ 你的答案 ↓



-- ↑ 答案结束 ↑



-- ==========================================================
-- 任务 5：统计每个处理人各处理了多少张工单
--   提示：只用工单表就够，不用 JOIN
--        SELECT w.处理人, COUNT(*) AS 张数 FROM study.维修工单 w
--        GROUP BY w.处理人 ORDER BY 张数 DESC
-- ==========================================================
--
-- ↓ 你的答案 ↓



-- ↑ 答案结束 ↑



-- ==========================================================
-- 任务 6（挑战）：每台设备的「最后一次报修日期」
--   提示：GROUP BY d.设备编号，取 MAX(w.报修日期)
--        日期要好看就套一层：TO_CHAR(MAX(w.报修日期),'YYYY-MM-DD')
--   难点：从没修过的设备（SB-004）要不要出现？出现了显示什么？
--        想想该用 JOIN 还是 LEFT JOIN，然后两种都试试看差别
-- ==========================================================
--
-- ↓ 你的答案 ↓



-- ↑ 答案结束 ↑



-- ==========================================================
PROMPT
PROMPT ===== 最后：两张表全部数据（对照用）=====
SELECT * FROM study.设备台账 ORDER BY 设备编号;
SELECT 工单号, 设备编号, TO_CHAR(报修日期,'YYYY-MM-DD') AS 报修日期, 故障现象, 处理人, 完成状态
FROM   study.维修工单 ORDER BY 工单号;
